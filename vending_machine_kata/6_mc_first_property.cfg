\* SPECIFICATION
\* Uncomment the previous line and provide the specification name if it's declared
\* in the specification file. Comment INIT / NEXT parameters if you use SPECIFICATION.

CONSTANTS
    customer_names <- const_customer_names
    vending_machine_names <- const_vending_machine_names
    min_coin_stock <- const_min_coin_stock
    max_coin_stock <- const_max_coin_stock
    min_product_stock <- const_min_product_stock
    max_product_stock <- const_max_product_stock
    products <- const_products
    cointypes <- const_cointypes

INIT Init
NEXT Next

PROPERTY
AssetValueIsConstant

\* INVARIANT
\* 